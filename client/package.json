{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "sh -c 'PID=$(lsof -tiTCP:5173 -sTCP:LISTEN || true); if [ -n \"$PID\" ]; then kill -9 $PID || true; fi'",
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint .",
    "prepreview": "sh -c 'PID=$(lsof -tiTCP:5173 -sTCP:LISTEN || true); if [ -n \"$PID\" ]; then kill -9 $PID || true; fi'",
    "preview": "vite preview"
  },
  "dependencies": {
    "@react-oauth/google": "^0.12.2",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "^7.9.5",
    "recharts": "^3.5.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "@vitejs/plugin-react": "^5.1.0",
    "eslint": "^9.39.1",
    "eslint-plugin-react-hooks": "^5.2.0",
    "eslint-plugin-react-refresh": "^0.4.24",
    "globals": "^16.5.0",
    "vite": "^7.2.2"
  }
}
